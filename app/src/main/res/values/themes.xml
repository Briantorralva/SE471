<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.VolleyBook" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/silver</item>
        <item name="android:windowBackground">@color/Ebony</item>
    </style>

    <style name="Theme.VolleyBook" parent="Base.Theme.VolleyBook" />

</resources>